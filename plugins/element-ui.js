import Vue from 'vue';

import {
    Avatar,
    Image,
    Pagination,
    Carousel,
    CarouselItem,
    Button,
    Loading,
    Tag,
    Link,
} from 'element-ui';

Vue.use(Avatar)
   .use(Image)
   .use(Pagination)
   .use(Carousel)
   .use(CarouselItem)
   .use(Button)
   .use(Loading)
   .use(Tag)
   .use(Link);
