import Vue from 'vue';

import {
    Avatar,
    Pagination,
    Empty,
    Carousel,
    Icon,
    Button,
    Spin
} from 'ant-design-vue/lib';

Vue.use(Avatar)
   .use(Pagination)
   .use(Empty)
   .use(Carousel)
   .use(Icon)
   .use(Button)
   .use(Spin);
